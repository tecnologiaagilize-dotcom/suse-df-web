{
  "buildCommand": "cd apps/web && npm install && npm run build && echo 'Build Complete v1.2.9-fix'",
  "outputDirectory": "apps/web/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}