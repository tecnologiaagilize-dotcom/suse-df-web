{
  "name": "suse-df-root",
  "private": true,
  "scripts": {
    "postinstall": "npm install --workspaces",
    "build": "cd apps/web && npm run build",
    "build:veritas": "cd apps/veritas-web && npm run build",
    "vercel-build": "cd apps/web && npm install && npm run build"
  },
  "engines": {
    "node": "20.x"
  },
  "workspaces": [
    "apps/*"
  ]
}
